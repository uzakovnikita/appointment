import { FC, PropsWithChildren } from 'react'
import { UI_VARIANT, UI_SIZE } from '..'

export const Chip: FC<
  PropsWithChildren<{ variant: UI_VARIANT; size: UI_SIZE }>
> = ({ variant, size, children }) => {
  return <div></div>
}
